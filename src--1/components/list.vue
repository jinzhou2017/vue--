<template>
  <div class="col-md-8">
    <h3 class="reply">评论回复：</h3>
    <h2 v-show="msg.length === 0">暂无评论，点击左侧添加评论！！！</h2>
    <ul class="list-group">
      <item v-for="(comment , index) in msg" :comment="comment" :remove-one='removeOne' :key="index" :index="index"></item>


    </ul>
  </div>
</template>
<script>
  import item from './item.vue'
  export default {
    props: {
      msg: {
        type: Array,
        required: true
      }
    },
    components: {
      item
    },
    methods: {
      removeOne (index) {
        this.msg.splice(index, 1)
      }
    }
  }

</script>
<style>
  li {
    transition: .5s;
    overflow: hidden;
  }

  .handle {
    width: 40px;
    border: 1px solid #ccc;
    background: #fff;
    position: absolute;
    right: 10px;
    top: 1px;
    text-align: center;
  }

  .handle a {
    display: block;
    text-decoration: none;
  }

  .list-group-item .centence {
    padding: 0px 50px;
  }

  .user {
    font-size: 22px;
  }

</style>
